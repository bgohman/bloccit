<%= form_for [@post, @post.questions.build], remote: true do |f| %>
  <% if @post.questions.build.errors.any? %>
    <div class="alert alert-danger">
      <h4>There are <%= pluralize(question.errors.count, "error") %>.</h4>
      <ul>
        <% question.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <%end %>
      <div class="form-group">
        <%= f.label "Ask a question" %>
        <%= f.text_field :body, rows: 8, class: 'form-control', placeholder: "Enter your question." %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    
<% end %>