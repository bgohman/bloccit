<p><%= @comment.body %></p>

<%= link_to "Edit", edit_post_comment_path(@post, @comment), class: 'btn btn-success' %>
<br><br>
<%= link_to "Delete", @comment, method: :delete, data: { confirm: "Are you sure?" }, class: 'btn btn-danger' %>